# The anchor across the published corpus

`inkfloor corpus --kind model`, bucket state of 8 August 2026.

The floor is a Delta between two derivations of one scan. It is only interpretable
against the anchor: the Delta between two checkpoints on one derivation. With one
segment carrying a floor pair, the anchor was four numbers. This is the anchor measured
on every segment in the bucket that carries one.

56 pairs across 55 segments, 55 of 55 measured, 0 skipped. 5.3 GB fetched.

| sample | pairs | median Δ@5% | min | max |
| --- | --- | --- | --- | --- |
| `PHerc0172` | 54 | 0.627 | 0.533 | 0.784 |
| `PHerc0500P2` | 2 | 0.781 | 0.577 | 0.985 |
| **all** | **56** | **0.627** | **0.533** | **0.985** |

mean 0.645, sd 0.081.
Widest tie band among the 56: 0.0193, so every Delta here is a fact about
the maps rather than an artefact of how ties break at the top-k boundary.

## Where the floor sits

The two derivation pairs on `PHerc0172/20251107110950-w064_20251107110950052_flatboi`
give Delta@5% of 0.620 and 0.713. Against these 56 anchors:

- 0.620 is the 46th percentile
- 0.713 is the 80th percentile

Changing which derivation of one scan a checkpoint runs on moves the ink map about as
much as a median change of checkpoint.

This does not enlarge the floor's own sample: one segment is still all the corpus
offers. It replaces the four numbers the floor used to be read against.

## Two pairs fail the alignment control

Both `PHerc0500P2` pairs agree better after a rigid 64 px translation than without it, which
54 of the 56 do not. `20250919184428-0500P2-wrap01_0919` overlaps at IoU 0.015 against a
chance level of 0.026, while its rank correlation across the whole map is 0.636: content that
correlates globally while its top-k does not overlap is what an offset looks like. Reported
separately as ScrollPrize/villa#1377, with the caveat that this control is also fooled by
periodic structure.

Excluding those two leaves 54 pairs, and the conclusion does not move: median 0.627 either
way, standard deviation tightens from 0.081 to 0.067, and the floor sits at the 46th and 81st
percentile instead of the 46th and 80th.
